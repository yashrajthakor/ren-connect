# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
# NOTE: package-lock.json is intentionally COMMITTED.
# The Docker build (see Dockerfile) runs `npm ci`, which requires it to be
# present in the build context. Ignoring it made deploys fail with
# `"/package-lock.json": not found` while building fine locally.
